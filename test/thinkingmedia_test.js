var grunt = require('grunt');

exports.thinkingmedia = {
    setUp: function (done) {
        done();
    },

    requiresDevOptions: function(test) {
        test.done();
    },

    requiresProdOptions: function(test) {
        test.done();
    }
};
